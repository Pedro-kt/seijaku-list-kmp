query GetRecentlyAddedManga($page: Int, $perPage: Int, $isAdult: Boolean) {
    Page(page: $page, perPage: $perPage) {
        media(type: MANGA, sort: ID_DESC, isAdult: $isAdult) {
            id
            title {
                romaji
                english
                native
            }
            coverImage {
                extraLarge
                large
                medium
                color
            }
            averageScore
            format
            chapters
            volumes
            genres
            isAdult
        }
    }
}
